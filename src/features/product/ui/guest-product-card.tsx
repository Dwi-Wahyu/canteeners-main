import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function GuestProductCard() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Guest Product Card</CardTitle>
            </CardHeader>
        </Card>
    )
}